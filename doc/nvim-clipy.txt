*nvim-clipy.txt*  System-wide clipboard history for Neovim

==============================================================================
CONTENTS                                                *nvim-clipy-contents*

    1. Introduction ............... |nvim-clipy-introduction|
    2. Why not neoclip.lua? ....... |nvim-clipy-vs-neoclip|
    3. Prerequisites ............... |nvim-clipy-prerequisites|
    4. Installation ............... |nvim-clipy-installation|
    5. Setup ............................. |nvim-clipy-setup|
    6. Commands ....................... |nvim-clipy-commands|
    7. Keymaps ....................... |nvim-clipy-keymaps|
    8. Lua API ........................... |nvim-clipy-api|

==============================================================================
1. Introduction                                    *nvim-clipy-introduction*

System-wide clipboard history for Neovim. Copy anything, anywhere on your
machine -- Neovim, your browser, Slack, a terminal -- and it's captured,
searchable, and re-copyable from inside Neovim via a Telescope picker with
preview.

It's backed by clipy (https://github.com/c12i/clipy-rust), a small daemon
that polls the OS clipboard continuously in the background, independent of
Neovim. nvim-clipy talks to it over its Unix socket using `vim.uv`.

==============================================================================
2. Why not neoclip.lua?                              *nvim-clipy-vs-neoclip*

neoclip.lua (https://github.com/AckslD/nvim-neoclip.lua) is a similar-
looking picker, but it's fundamentally different: it hooks `TextYankPost`,
so it only records things yanked or deleted *inside Neovim*. Copy something
in your browser, a terminal, Slack, anywhere outside Neovim, and it's
invisible to neoclip. Its history also lives in Neovim's own memory by
default, so it resets every time Neovim closes unless you turn on
persistent history.

nvim-clipy's history comes from the daemon polling the real system
clipboard, not a Neovim event, so it captures everything you copy anywhere
on the machine. Because that capture is a separate process from Neovim, it
persists across Neovim restarts (and reboots too, if you run the daemon
under a supervisor, see |nvim-clipy-prerequisites|) and is shared across
every Neovim instance you have open, not just the one that made the copy.

==============================================================================
3. Prerequisites                                  *nvim-clipy-prerequisites*

The `clipy` daemon is published on crates.io and installs via cargo:
>
    cargo install clipy
<
nvim-clipy starts the daemon for you. On `VimEnter` it checks whether
`clipy watch` is already reachable and, if not, spawns it detached so it
keeps running after Neovim closes. If `clipy` isn't on `$PATH`, you'll get
a warning instead of an error, pointing back at `cargo install clipy`.

If you'd rather the daemon run independently of Neovim entirely --
surviving reboots, capturing clipboard changes even when Neovim is never
opened -- start it yourself under a process supervisor instead (on macOS, a
launchd LaunchAgent with `RunAtLoad`/`KeepAlive` works well):
>
    clipy watch > /tmp/clipy.log 2>&1 &
<

==============================================================================
4. Installation                                    *nvim-clipy-installation*

With lazy.nvim:
>lua
    -- lua/plugins/nvim-clipy.lua
    return {
      {
        "c12i/nvim-clipy",
        dependencies = { "nvim-telescope/telescope.nvim" },
        -- Must load at startup, not lazily on keypress: the daemon-
        -- autostart autocmd lives in plugin/nvim-clipy.lua, which only
        -- gets sourced once the plugin loads. Declaring only `keys` below
        -- (without `lazy = false`) would defer loading until the first
        -- keypress -- past VimEnter, so the autocmd never gets a chance
        -- to fire and the daemon never auto-starts.
        lazy = false,
        keys = {
          {
            "<leader>sy",
            function()
              require("nvim-clipy.telescope").pick()
            end,
            desc = "Clipboard history",
          },
        },
        config = function()
          require("nvim-clipy").setup()
        end,
      },
    }
<

==============================================================================
5. Setup                                                  *nvim-clipy-setup*

>lua
    require("nvim-clipy").setup({
      -- socket_path defaults to your OS's per-user data dir + "clipy-rust/clipy.sock"
      -- (same resolution as the daemon: respects $CLIPY_DATA_DIR if set, see
      -- clipy-rust/src/daemon.rs), e.g. on macOS:
      -- "~/Library/Application Support/clipy-rust/clipy.sock"
      socket_path = "...",
      history_limit = 30, -- default shown
    })
<

==============================================================================
6. Commands                                            *nvim-clipy-commands*

    :ClipyList                     List recent clipboard history entries.
    :ClipyShow {id}                Show an entry's full content in a float.
    :ClipyCopy {id}                Copy an entry back to the clipboard.
    :ClipyDelete {id}               Delete one entry.
    :ClipyClear                    Delete all history.
    :ClipyStatus                   Show daemon status.
    :ClipyKill                     Stop the running daemon. It restarts
                                     automatically next time Neovim starts.
    :ClipyPick                     Pick an entry via Telescope, with a
                                     content preview (requires telescope.nvim).
                                     Also available as `:Telescope nvim-clipy`
                                     once the extension is loaded with
                                     `require("telescope").load_extension("nvim-clipy")`.

`{id}` accepts a unique prefix, same as the `clipy` CLI.

In the `:ClipyPick` picker: `<CR>` copies the selected entry back to the
clipboard, `<C-d>` deletes it.

==============================================================================
7. Keymaps                                              *nvim-clipy-keymaps*

No default keymaps are set. If you're not using lazy.nvim's `keys` spec
(shown in |nvim-clipy-installation|), a plain keymap works too:
>lua
    vim.keymap.set("n", "<leader>sy", function()
      require("nvim-clipy.telescope").pick()
    end, { desc = "Clipboard history" })
<

==============================================================================
8. Lua API                                                  *nvim-clipy-api*

>lua
    require("nvim-clipy").setup(opts)
    require("nvim-clipy").list()
    require("nvim-clipy").show(id)
    require("nvim-clipy").copy(id)
    require("nvim-clipy").delete(id)
    require("nvim-clipy").clear()
    require("nvim-clipy").status()
    require("nvim-clipy").kill()
    require("nvim-clipy").fetch(opts, callback) -- opts = { limit, query }
<

`fetch()` is async: `callback(entries)` fires once the daemon responds,
where each entry is `{ id, content, created_at, updated_at }`. All other
functions are fire-and-forget and report errors via |vim.notify()|.

vim:tw=78:ts=8:ft=help:norl:
